[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "pip install torch --index-url https://download.pytorch.org/whl/cpu",
  "pip install -r requirements.txt"
]

[start]
cmd = "python run.py"
